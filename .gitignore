# Build output (build/, build_thingy/, build_dk/, build-*/ — at any depth)
build/
build-*/
build_*/

# Compiled host tools (sources are committed, binaries are not)
tools/aether_conv
tools/aether_test
tools/aether_verify
tools/p9do

# Licensed Nordic DECT NR+ PHY modem firmware — NEVER commit (per license agreement)
dectfw/

# Session logs / scratch
*.log
*.exit

# Local editor / agent config
.claude/

# Editor swap/backup files
*.swp
*.swo
*~
