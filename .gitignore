# Build output (build/, build_thingy/, build-*/ — at any depth)
build/
build-*/
build_*/

# Compiled host tools (the source is committed, the binary is not)
tools/aether_conv
tools/aether_test

# Licensed Nordic DECT NR+ modem firmware — NEVER commit (per license agreement)
dectfw/

# Session logs / scratch
*.log
*.exit

# Python / tooling
*.pyc
.venv/

# Local editor / agent config
.claude/

# Editor swap/backup files
*.swp
*.swo
*~
tools/p9do
tools/aether_verify
