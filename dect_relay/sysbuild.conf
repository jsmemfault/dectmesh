# Build the nRF5340 network-core image as a Bluetooth HCI controller (IPC radio),
# so the application core's BLE host has a controller to talk to.
SB_CONFIG_NETCORE_IPC_RADIO=y
SB_CONFIG_NETCORE_IPC_RADIO_BT_HCI_IPC=y
